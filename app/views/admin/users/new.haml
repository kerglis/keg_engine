%h1= tN(User)

= simple_form_for(@user, :url => collection_url, :html => {:class => "form-horizontal"}) do |f|
  = render :partial => "form", :locals => { :f => f }
  = render :partial => "admin/shared/new_resource_links"